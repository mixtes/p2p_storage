/**
 * Friend Storage UI
 *
 * DOM bindings for the friend-storage favour cards.
 * Will render:
 *  - Active favours (who you're storing for, who stores for you)
 *  - Space used for friends vs. space friends use for you
 *  - Upload-to-friend button
 *  - Retrieve-my-files button
 */

const $ = (id) => document.getElementById(id)

export function init () {
  // TODO: bind buttons, render initial state
}

export function render (favours) {
  // TODO: render favour list into #view-friend-storage
}
