/**
 * Replication UI
 *
 * DOM bindings for the replication dashboard.
 * Will render:
 *  - Space offered / space used per peer
 *  - Replication health indicators
 *  - File/folder selector for what to replicate
 *  - Agreement management (add/remove peers)
 */

const $ = (id) => document.getElementById(id)

export function init () {
  // TODO: bind buttons and render initial state
}

export function render (agreements) {
  // TODO: render agreement list into #view-replication
}
